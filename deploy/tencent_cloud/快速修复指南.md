# 快速修复指南 - 价格监控API 500错误

## 方式1：手动复制（最简单）

### 在本机操作：
1. 找到文件：`e:\E\任务\task-platform\web\backend\api\yantai_db.py`
2. 复制此文件

### 在服务器上操作：
1. 登录服务器（140.143.125.234）
2. 打开文件：`C:\taskplatform\web\backend\api\yantai_db.py`
3. 用本机的文件内容替换服务器文件内容
4. 保存文件

### 重启服务：
```powershell
cd C:\taskplatform
taskkill /F /IM python.exe
python -m uvicorn web.backend.main:app --host 0.0.0.0 --port 8000
```

---

## 方式2：使用部署脚本

### 在服务器上运行：
```batch
e:\E\任务\task-platform\deploy\tencent_cloud\deploy-yantai-fix.bat
```

---

## 验证修复
访问：http://140.143.125.234/
- 应该显示历史数据总量：10764 条记录
- 覆盖 415 个交易日

---

## 修复内容说明
修改了 7 个 API 端点的错误处理：
- 当 Supabase 连接失败时，自动回退到 SQLite
- 添加日志记录便于调试

修改文件：`web/backend/api/yantai_db.py`

每个端点添加了：
```python
try:
    if supabase.url:
        result = supabase.get_xxx()
        if result:
            return result
except Exception as e:
    logger.warning(f"Supabase查询失败，回退SQLite | {e}")
# 继续执行 SQLite 代码...
```
