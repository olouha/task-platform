# 钢筋价格截图识别 OCR 依赖（RapidOCR，基于 PP-OCR 模型，不依赖大模型）
#
# 重要：onnxruntime 必须用 1.16.x 且 numpy<2.0（已验证组合）
#   - 新版 onnxruntime（1.20+）在部分 Windows 环境 DLL 加载失败
#   - onnxruntime 1.16 是 numpy 1.x ABI 时代编译，配 numpy 2.x 会 _ARRAY_API 崩溃
#   - 所以三者必须一起 pin：onnxruntime==1.16.3 + numpy<2.0
rapidocr_onnxruntime>=1.3.0,<2.0
onnxruntime==1.16.3
numpy<2.0
pillow>=9.0.0

# 可选回退引擎（中文识别率低，仅 RapidOCR 不可用时兜底）：
# pytesseract（还需系统安装 Tesseract-OCR，Windows 路径 C:\Program Files\Tesseract-OCR\）
