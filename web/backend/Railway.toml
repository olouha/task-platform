[railway]
name = "task-platform-backend"
version = 2

[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m pip install fastapi uvicorn playwright && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthCheckPath = "/health"
region = "singapore"