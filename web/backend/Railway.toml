[railway]
name = "task-platform-backend"
version = 2

[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install fastapi uvicorn[standard] playwright && python -m uvicorn main:app --host 0.0.0.0 --port $PORT"
healthCheckPath = "/health"
region = "singapore"