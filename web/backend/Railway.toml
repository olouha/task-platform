[railway]
name = "task-platform-backend"
version = 2

[build]
builder = "nixpacks"
buildCommand = "pip install -r requirements.txt"

[deploy]
startCommand = "bash web/backend/start_simple.sh"
healthCheckPath = "/health"
region = "singapore"
rootDirectory = "web/backend"