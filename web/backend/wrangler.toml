"""
Cloudflare Workers 定时触发器配置
wrangler.toml
"""

name = "taskplatform-price-scraper"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[triggers]
crons = ["0 8 * * *"]  # 每天早上8点 UTC = 下午4点北京时间

[vars]
ENVIRONMENT = "production"